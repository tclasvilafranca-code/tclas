DATABASE_URL="postgresql://teorico48:teorico48@localhost:5433/teorico48"
JWT_SECRET="cambia-esto-por-un-secreto-largo-y-aleatorio"
PORT=4001
CLIENT_URL="http://localhost:5174"

# Stripe (pago unico "Pack 48h"). Deja vacio para desarrollar sin pagos.
STRIPE_SECRET_KEY=""
STRIPE_PACK_PRICE_ID=""
STRIPE_WEBHOOK_SECRET=""

# Email transaccional (recuperacion de contrasena) via Resend (resend.com).
# Sin clave, los emails se escriben en la consola del servidor en su lugar,
# util para desarrollar sin depender de un proveedor externo.
RESEND_API_KEY=""
MAIL_FROM="Teórico48 <onboarding@resend.dev>"

# Contraseña del panel de administrador (/admin) para generar códigos de
# acceso. El registro está cerrado: solo se puede crear cuenta con un
# código válido generado desde ahí.
ADMIN_PASSWORD="cambia-esto-por-una-contrasena-fuerte"
